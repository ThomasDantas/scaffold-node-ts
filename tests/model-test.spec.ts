describe('Test', () => {
  beforeAll(() => {

  })

  beforeEach(() => {
  })

  it('Base Test', () => {
    expect(1).toBe(1)
  })
})

/* sut = O System Under Test (SUT) de uma perspectiva de teste de unidade representa todos os atores
 (ou seja, uma ou mais classes) em um teste que não são mocks ou stubs. */
